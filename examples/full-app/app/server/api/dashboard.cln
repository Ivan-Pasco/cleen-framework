// Dashboard statistics API

endpoints:
	[user, admin] GET "/api/dashboard":
		string tasks = Task.find:
			order: status asc
		string projects = Project.find:
			order: name asc
		return jsonResponse("{\"ok\": true, \"tasks\": " + tasks + ", \"projects\": " + projects + "}")
