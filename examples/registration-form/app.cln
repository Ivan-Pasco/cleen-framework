// Registration Form Example
// A complete frontend example using Frame Framework plugins

plugins:
	frame.ui
	frame.httpserver

config:
	name: "Registration Form"
	version: "1.0.0"

server port=3000:
	static: "./public"
