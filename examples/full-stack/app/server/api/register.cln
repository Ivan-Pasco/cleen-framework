// Register endpoint

endpoints:
	POST "/api/register":
		string body = body()
		string hash = _auth_hash_password(body)
		User.insert:
			password_hash = hash
			role = "user"
			active = true
		return jsonResponse("{\"ok\": true, \"message\": \"registered\"}")
