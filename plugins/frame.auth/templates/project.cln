// Project Configuration
// Application metadata and build settings

config:
	name: "{{PROJECT_NAME}}"
	version: "0.1.0"
	description: "A Clean Language application"

server:
	port: 3000
	host: "localhost"

build:
	output: "dist"
	target: "web"
