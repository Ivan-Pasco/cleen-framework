// Full App Configuration

config:
	project:
		name = "full-app"
		version = "1.0.0"
		description = "Full application with all features"

	build:
		output = "dist"

	server:
		port = 3000

	auth:
		strategy = "session"
		secret = env("APP_SECRET")
