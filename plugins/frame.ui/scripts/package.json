{
  "name": "@clean-framework/frame-ui-canaries",
  "version": "0.0.0",
  "private": true,
  "description": "Layer 2 canary runner for frame.ui — instantiates compiler canaries in headless Chromium and diffs stdout against the canary's expected block.",
  "type": "module",
  "scripts": {
    "canaries": "node run_canaries.mjs"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
