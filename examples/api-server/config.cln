// API Server Configuration

config:
	project:
		name = "api-server"
		version = "1.0.0"
		description = "REST API server with authentication"

	build:
		output = "dist"

	server:
		port = 3000

	auth:
		strategy = "jwt"
		secret = env("JWT_SECRET")
