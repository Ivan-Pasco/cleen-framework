// Admin user management endpoint

endpoints:
	[admin] DELETE "/api/admin/users/:id":
		string id = param("id")
		User.delete:
			where:
				id = id
		return jsonResponse("{\"ok\": true, \"message\": \"user deleted\", \"id\": \"" + id + "\"}")
