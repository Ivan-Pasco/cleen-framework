// Register endpoint

endpoints:
	POST "/api/register":
		string body = body()
		string hash = auth.password.hash(body)
		User.insert:
			password_hash = hash
			role = "user"
			active = true
		return jsonResponse("{\"ok\": true, \"message\": \"registered\"}")
