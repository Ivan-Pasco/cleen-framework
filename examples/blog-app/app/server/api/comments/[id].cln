// Single comment endpoints

endpoints:
	[user, admin] DELETE "/api/comments/:id":
		string id = param("id")
		string comment = Comment.first:
			where:
				id = id
		if comment == "" or comment == "null"
			return notFound()
		Comment.delete:
			where:
				id = id
		return jsonResponse("{\"ok\": true}")
