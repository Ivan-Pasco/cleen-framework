// Auth Guards Configuration

config:
	project:
		name = "auth-guards"
		version = "1.0.0"
		description = "Auth guards and role-based access control demo"

	build:
		output = "dist"

	server:
		port = 3000

	auth:
		strategy = "jwt"
		secret = env("JWT_SECRET")
