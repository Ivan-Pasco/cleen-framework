// Single comment API

endpoints:
	[user, admin] DELETE "/api/comments/:id":
		string id = param("id")
		Comment.delete:
			where:
				id = id
		return jsonResponse("{\"ok\": true, \"message\": \"comment deleted\", \"id\": \"" + id + "\"}")
