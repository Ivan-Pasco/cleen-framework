// Single comment API

endpoints:
	DELETE "/api/comments/:id":
		_auth_require_auth()
		string id = param("id")
		Comment.delete:
			where:
				id = id
		return jsonResponse("{\"ok\": true, \"message\": \"comment deleted\", \"id\": \"" + id + "\"}")
