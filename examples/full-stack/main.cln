package: FullStack
	version: "1.0.0"
	description: "Full-stack app with all Frame features"

	target: web
		plugins: [frame.ui, frame.server, frame.data, frame.auth]
		entry: app/ui/web/pages/index.html

	server:
		port: 3000

	auth:
		strategy: session
		secret: env.get("APP_SECRET")
