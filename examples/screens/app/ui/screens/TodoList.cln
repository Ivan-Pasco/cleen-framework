// TodoList Screen - Interactive todo list component
// Demonstrates state with text input and list rendering

state:
	string items = ""
	integer itemCount = 0

functions:
	string render()
		html:
			<div class="todo-container">
				<h2 id="todo-title">Todo List ({itemCount.toString()} items)</h2>
				<div class="todo-input">
					<input type="text" class="todo-input-field" placeholder="Add new item...">
					<button class="btn-add">Add</button>
				</div>
				<div class="todo-items" id="todo-list">
					<pre>{!items}</pre>
				</div>
				<button class="btn-clear">Clear All</button>
			</div>

	handle_event_0()
		string newItem = _ui_eventValue()
		if newItem != ""
			items = items + newItem + "\n"
			itemCount = itemCount + 1
			integer s = _ui_updateElement("#todo-title", "Todo List (" + itemCount.toString() + " items)")
			s = _ui_updateElement("#todo-list", "<pre>" + items + "</pre>")

	handle_event_1()
		items = ""
		itemCount = 0
		integer s = _ui_updateElement("#todo-title", "Todo List (0 items)")
		s = _ui_updateElement("#todo-list", "<pre></pre>")

start:
	integer s = 0
	s = _ui_onEvent(".btn-add", "click", 0)
	s = _ui_onEvent(".btn-clear", "click", 1)
