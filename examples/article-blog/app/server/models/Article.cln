// Article model
// Schema for the Article data type

functions:
	string getArticleSchema()
		return "CREATE TABLE IF NOT EXISTS articles (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, slug TEXT UNIQUE, excerpt TEXT, content TEXT, category TEXT, read_time INTEGER DEFAULT 5, created_at DATETIME DEFAULT CURRENT_TIMESTAMP)"

start:
	printl("Article model loaded")
